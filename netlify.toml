[build]
command = "npm run build"
publish = "dist"

[[redirects]]
  from = "/api/*"
  to = "https://football-data.org"
  status = 200
  force = true
  headers = { X-Auth-Token = "${env:VITE_FOOTBALL_API_KEY}" }